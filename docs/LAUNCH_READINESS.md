# Kinwin Launch Readiness

Read-only audit as of main `d8ec43f` (2026-08-13), with the Apple/iOS section below corrected 2026-08-23 — see `docs/AI_HANDOFF.md` for the current, short-form state and for anything shipped since this audit's baseline. This is the authoritative detailed-evidence document for everything else — where it conflicts with older docs, trust this one and the evidence cited here. See [Known documentation drift](#known-documentation-drift) for specifics.

Method: evidence from code, migrations, deployed Edge Functions, tests, and merged PRs — not doc claims. Preview/prototype routes (`app/challenge-ux-preview/*`, `app/social-onboarding-preview/*`, `app/social-preview/*`, and the legacy `app/challenge/check-in.tsx` fixture screen) are excluded from "shipping scope": none of them are reachable from `app/_layout.tsx`'s root `<Stack>`, which only registers `index`, `auth`, `invite`, `account`, `home`, `create`, `challenge`.

## Current shipping scope

**Auth** — email/password sign-up, sign-in, session persistence, sign-out, password reset (`contexts/auth-context.tsx`, `app/auth/forgot-password.tsx`, `app/auth/reset-password.tsx`). No OAuth, no magic link.

**Challenge lifecycle, end to end, server-authoritative:**
- Draft creation → 12-screen onboarding wizard (`app/create/*`), server RPC `prepare_challenge_from_draft` turns a draft into a pending commitment; only one pending commitment per owner is enforced server-side (`20260808000000_one_pending_commitment_per_owner.sql`).
- Stripe TEST payment-method setup via PaymentSheet (`app/account/payment-setup.tsx`), SetupIntent created/reused server-side, webhook-verified (`create-consequence-setup-intent`, `stripe-consequence-webhook`).
- Activation (`app/account/pending-commitment.tsx` → `20260811000000_full_activation.sql`) re-validates timezone and payment authorization server-side, not just trusting the client.
- Real check-ins write actual `check_in_events` rows via `append-check-in-event`, not local state. Period generation is SQL-only by design (`domain/challenge/check-in/periods.ts` deliberately leaves `GeneratePeriods` unimplemented client-side "so no competing TypeScript algorithm can ever drift from that one SQL function").
- `domain/challenge/results.ts`'s `evaluateChallenge` is a real, complete evaluator (build/cut-back/stop directions) — **not** the stub `docs/BACKEND_IMPLEMENTATION_PLAN.md` still describes.
- Completion is cron-scheduled (`kinwin-challenge-completion`, every 15 min) with an authenticated on-open fast path (`finalize-challenge`) as "defense in depth, not authoritative."
- Failure charging: `_shared/consequence-payment` — idempotency key per obligation, DB-unique constraints on `consequence_id` and `stripe_payment_intent_id`, and a synchronous Stripe "succeeded" response is deliberately **not** trusted — only the HMAC-verified webhook persists paid truth. Cron-scheduled hourly.
- Recipient invitation: 43-char bearer token, only its SHA-256 hash is persisted, token rotates (invalidating the old one) whenever a new one is issued (`create-recipient-invitation`, `create-organizer-invitation`).
- Reward fulfillment: Tremendous integration is **hard-coded to sandbox** — `readTremendousSandboxConfig` rejects any base URL other than `https://testflight.tremendous.com` and any API key not prefixed `TEST_`. Automated via cron (fulfillment every 30 min, reconciliation every 15 min), with real idempotency (`unique(idempotency_key)`, `unique(consequence_id)`, `unique(provider_order_id)`, `unique(provider_reward_id)`) and staged retry/terminal-failure handling.
- All of the above have unit tests (Node `--test`, fake adapters) and/or pgTAP-style SQL fixtures in `supabase/tests/`, wired into `package.json`'s `test`/`test:e2e` scripts — not orphaned.

**Personal Playbook** — fully implemented (create/edit/archive/delete lessons), linked from the completion screen and the profile tab.

**Kin (social) — real, ~1–5 days old at audit time:** friend connections (search by name/email or Kin code, request/accept/decline/remove/block), a shared activity feed, and reactions (`respect|nice|worth_it|ouch|brutal`) — all backed by real tables, RLS, and SECURITY DEFINER RPCs (`20260814000000_kin_connections.sql`, `20260815000000_social_activity.sql`, `20260817000000_kin_search_and_current_state.sql`), reachable from the real `Home → Kin` tab (`app/home/kin.tsx`). Visibility is **binary today**: any accepted Kin sees all of your activity and current-challenge state — there is no per-challenge audience selector in production (that model exists only in the unreachable prototype, see below).

**Beta release infrastructure** — EAS project linked (`@kinwin/kinwin-mobile`), `preview` environment fully configured (Supabase TEST URL/key, Stripe TEST publishable key, and a live EAS Hosting origin), a stable beta web host at `https://kinwin-beta.expo.app` (EAS Hosting, HTTPS-verified: root and `/invite/<token>` both return 200 — confirming Expo Router SPA fallback works there), and a manual GitHub Actions release workflow (`.github/workflows/eas-beta-release.yml`) that authenticates, links, configures environment, deploys the web host, and attempts the iOS build. **Currently blocked** at Apple credentials — see [Internal iPhone beta gate](#internal-iphone-beta-gate). **2026-08-15 beta web regression, fixed in the pipeline:** a stale Metro bundler transform cache caused the "real" configured web export to silently reuse the earlier config-less "claim the origin" export's cached bundle, so a deploy could ship a build with no Supabase config inlined even though the workflow's own HTTP-200 origin checks passed — those checks only prove the server answers, not that the served build actually works. The release pipeline now carries two permanent safeguards against this: (1) `--clear` on the real configured export, so it can never reuse another export's cache, and (2) a pre-deploy step that verifies the exported client bundle actually contains the TEST Supabase project configuration, failing the deploy before it ships if not. HTTP-200 routing checks remain useful but are treated as necessary, not sufficient, going forward. Each future beta web release should still be functionally spot-checked (Sign in reaches "Not connected yet" only if genuinely unconfigured) after deploying, the same as any release.

### Explicitly a separate, disconnected prototype — not shipping product

`domain/social/`, `lib/social/`, `components/social/`, `app/social-preview/*`, `app/social-onboarding-preview/*`, and `app/challenge-ux-preview/*` were built in short, self-contained bursts (the social prototype: 10 commits, all dated 2026-08-06), operate entirely on in-memory fixtures, and are not imported by any reachable app code. This is what `SOCIAL_V1_SPEC.md`'s "implementation has not started" line is describing, and it's still true for *that specific code path* — even though the real Kin feature it inspired has since shipped through a completely different implementation. `docs/SOCIAL_ONBOARDING_UX.md` and `docs/SOCIAL_UX_V1.md` correctly self-label as prototypes; only `SOCIAL_V1_SPEC.md`'s top-line status is misleading once you don't know about the real Kin feature.

## Internal iPhone beta gate

*Only what's needed to get the real app installed and exercised on an iPhone.*

**Corrected 2026-08-23 — see `docs/AI_HANDOFF.md`:** Apple Developer Program enrollment is
**APPROVED** (founder-confirmed). This table previously described it as pending identity
verification; that was accurate as of 2026-08-13/16 and is no longer true. A prior version of
Kinwin has also already been installed and run on the founder's physical iPhone (founder-confirmed;
the repository has no evidence of how that earlier build was distributed, so that mechanism is not
recorded here). The rows below describe the narrower, still-open question: current EAS-side signing
credential state, which has not been re-tested since Apple approval.

| Item | Status |
|---|---|
| Apple Developer Program enrollment | **RESOLVED — APPROVED.** No longer a blocker. |
| One-time interactive EAS credentials setup | **CURRENT EAS SIGNING CREDENTIAL STATE: NOT YET RE-VERIFIED.** As of the most recent run (2026-08-16, before Apple approval was confirmed), `eas build` correctly attempted EAS-managed remote iOS credentials (`✔ Using remote iOS credentials (Expo server)`) but failed non-interactively because none existed yet: *"EAS CLI couldn't find any credentials suitable for internal distribution. Run this command again in interactive mode."* That was an EAS-provisioning fact, not an Apple-enrollment fact. It has not been re-tested since. One founder-run `eas credentials` (or one interactive `eas build`) — Apple ID + 2FA, EAS auto-generates the actual certificate/profile — is still expected to permanently unblock every future *non-interactive* run of the existing GitHub Actions workflow, but re-trigger the workflow first to get current evidence rather than assuming the 2026-08-16 result still holds. |
| Test device registration | **STATUS UNKNOWN — NOT RE-VERIFIED.** `eas.json`'s `beta` profile is `"distribution": "internal"` with `"simulator": false` — a real-device ad-hoc build, which needs the target iPhone's UDID registered (`eas device:create`). No record of this exists in this repository's GitHub Actions history, but a prior Kinwin build has run on the founder's iPhone before (founder-confirmed) via a mechanism this repo doesn't have evidence for — that may mean this step was already done outside what's recorded here. Re-triggering the workflow will surface the real current state. |
| A build that actually completes | Chained to the two items above — re-trigger `.github/workflows/eas-beta-release.yml` and read its actual current result rather than assuming the 2026-08-16 failure still applies. |
| Universal Links end-to-end on a real device | **INTERNAL BETA BLOCKER, conditional** — `docs/IOS_BETA_BUILD.md`'s own physical-device test sequence includes opening `/invite/<token>` links with the app installed. This needs the real `KINWIN_APPLE_TEAM_ID` to generate and deploy the final AASA at `kinwin-beta.expo.app` — not itself blocked on enrollment anymore (enrollment is approved), but the Team ID hasn't been pulled into this repo/deployment yet. Until then, the `kinwin://` custom scheme still works for manual testing, so this doesn't have to block *first install*, only the *full* real-flow walkthrough. |
| `ITSAppUsesNonExemptEncryption` | **Not** a blocker here — EAS `internal` distribution is ad-hoc (direct `.ipa` install), it never goes through App Store Connect processing, so this warning is inert until the first TestFlight/App Store Connect upload. See External beta gate. |

This table is about **first install only** — getting the real app onto an iPhone at all. It is not the same as being able to exercise the full failure-to-reward mechanic. Apple is the blocker to getting the real native app installed; Tremendous Testflight runtime configuration (below) is now configured but its real chain still needs walking once a device build exists; AASA/Universal Links are needed specifically for the real invitation-link native handoff, not for the rest of the app.

### Full core-loop verification gate

Getting the app installed does **not** mean the whole Kinwin mechanic is verifiable yet. Everything up to and including a Stripe TEST consequence charge is testable once the app is on a device. The failure → reward half is separately blocked:

| Item | Status |
|---|---|
| Tremendous Testflight runtime configuration | **CONFIGURED — not yet end-to-end verified.** The Tremendous *implementation* is done and deployed (`_shared/tremendous/adapter.ts`, `scheduled-fulfill-rewards`, `scheduled-reconcile-rewards`, all cron-wired), and the four required runtime values — `TREMENDOUS_API_BASE_URL` (`https://testflight.tremendous.com`), a `TEST_`-prefixed `TREMENDOUS_API_KEY`, `TREMENDOUS_FUNDING_SOURCE_ID`, and `TREMENDOUS_CAMPAIGN_ID` (`readTremendousSandboxConfig`, `supabase/functions/_shared/tremendous/adapter.ts:21-27`) — are now set on the hosted TEST project. What's still unverified is the real chain: no `completed_failure` challenge has yet been walked through Stripe TEST charge → Tremendous TEST order → reconciliation → organizer Open reward on the hosted project. **No production Tremendous configuration was used — the same TEST/sandbox values documented in `docs/BETA_TEST_ENVIRONMENT.md`'s "Tremendous Testflight secrets" table.** |

Do not describe the Kinwin mechanic as end-to-end verified until the full chain (`completed_failure` → Stripe TEST charge → `reward_fulfillment_pending` → Tremendous TEST order → reconciliation → organizer Open reward) has actually been observed once on the hosted project.

## External beta gate

*Items needed before giving it to actual outside testers (beyond the founder/close circle testing via ad-hoc distribution).*

| Item | Status |
|---|---|
| Distribution model decision | **PRODUCT DECISION.** Ad-hoc "internal" distribution caps at ~100 registered devices per Apple membership year and needs each tester's UDID collected manually; TestFlight supports up to 10,000 external testers with no per-device registration, but requires an App Store Connect app record and export-compliance answers. Decide before scaling past a handful of testers. |
| `ITSAppUsesNonExemptEncryption` (`ios.config.usesNonExemptEncryption` in `app.config.js`) | **RESOLVED.** Set to `false`, justified by direct dependency/code inspection (not assumed): the app's only network traffic is HTTPS/TLS to Supabase and Stripe (OS-handled, exempt); Stripe React Native's native dependency graph (StripeCore/StripeUICore/StripePayments/StripePaymentsUI/StripeApplePay) relies on TLS and Apple Pay's own on-device encryption, not a bundled custom cipher; the only crypto API used in application code is `expo-crypto`'s `randomUUID()` for idempotency keys (standard system randomness, not confidentiality encryption); recipient/organizer token SHA-256 hashing happens entirely server-side, never in the app binary; no SecureStore/Keychain wrapper, VPN, DRM, or messaging-encryption SDK exists anywhere in the dependency tree. Verified the evaluated Expo config actually contains `{ usesNonExemptEncryption: false }`. See `docs/PRODUCT_STATUS.md` §13. |
| App Store Connect app record | **EXTERNAL BETA BLOCKER** if going the TestFlight route (not needed for continued ad-hoc). Does not exist yet — confirmed no App Store metadata/screenshot/privacy-label files anywhere in the repo. |
| Password reset | **RESOLVED — REAL HOSTED ROUND TRIP VERIFIED 2026-08-15.** Real Supabase `resetPasswordForEmail`/`setSession`/`updateUser` flow shipped across `app/auth/forgot-password.tsx` and `app/auth/reset-password.tsx`, enumeration-safe throughout, using the implicit auth flow deliberately (see `lib/supabase/client.ts`). The reset-password URL (`https://kinwin-beta.expo.app/auth/reset-password`) is on the hosted TEST Supabase project's Auth → Redirect URLs allowlist. The founder has manually completed a full real round trip against the hosted TEST project — request reset, receive email, open link, reset password — and confirmed it worked end to end. See `docs/PRODUCT_STATUS.md` §1. |
| Support/contact surface | **RESOLVED.** `EXPO_PUBLIC_SUPPORT_EMAIL` is now `support@kinwin.app` — the real, shipped "Contact Kinwin" row (`app/account/index.tsx`, `lib/support/config.ts`) opens a `mailto:` to it. Also wired into beta build config: `scripts/beta-public-config.cjs` now requires a real-looking support email for any `beta`-profile build (fails closed if missing, matching the other required public values), and the manual `eas-beta-release.yml` workflow sets it in the EAS preview environment and both local pre-flight steps that need it. See `docs/PRODUCT_STATUS.md` §1. |
| Fixture-driven "Progress" surface | **RESOLVED.** The screen was moved out of `app/` (to `fixtures/previews/home-progress-preview.tsx`) and its route registration removed from `app/home/_layout.tsx` — it is no longer reachable from the shipping app at all. No real progress/statistics feature was built to replace it; that remains a separate, undecided future item. See `docs/PRODUCT_STATUS.md` §5. |
| Consequence-consent copy, legal-reviewed | **NOT an external-beta blocker — corrected classification.** Functional disclosure UI already exists in two places — a required outcomes-acknowledgment checkbox on `app/create/review.tsx`, and a separate consent screen at `app/account/payment-setup.tsx`. Kinwin's current external beta is deliberately Stripe TEST mode and Tremendous sandbox, with no real money and no real reward value; for that model, the concrete external-beta requirement is honest UX that clearly tells testers this is TEST payment behavior and no real money will be charged — see the TEST-mode payment disclosure row below, which is the real near-term task and stays a blocker. Final, real-money legal review of this copy is **not itself required to run an external TEST beta** as long as that TEST-mode disclosure is clear. It becomes genuine legal/business readiness work — consequence/payment/reward wording, refund/dispute expectations, and any custom-Terms decision — before a **public, real-money** launch; no official Apple source says this copy must be lawyer-reviewed to submit for App Review, so this row should not be read as an Apple requirement. |
| TEST-mode payment disclosure copy | **RESOLVED.** A shared notice (`BETA_PAYMENT_TEST_MODE_NOTICE`, `lib/copy/beta-payment-notice.ts`) — *"Beta test: payments are simulated. No real money will be charged."* — now appears at both points in the real journey where a tester is asked to commit to or authorize the stake: on the final challenge review (`app/create/review.tsx`, next to the stake/consequence summary) and as the first line of the payment-setup consent screen (`app/account/payment-setup.tsx`), before Stripe's PaymentSheet opens. The same line was also added to `app/account/payment-recovery.tsx`'s consent screen for consistency, since it opens the same PaymentSheet. Disclosure/copy only — no payment semantics, Stripe/Tremendous calls, or backend logic changed. |
| TEST-mode Stripe/Tremendous (as opposed to production) | **Not a blocker — intentional.** Staying on TEST/sandbox rails for the external beta is the correct, deliberate choice; `scripts/beta-public-config.cjs` and `readTremendousSandboxConfig` both actively reject anything else at build/runtime. Only becomes relevant at public launch. The absence of production rails is not itself a blocker; the absence of in-app disclosure of that fact is (see the row above). |
| Tremendous Testflight runtime configuration (the four secrets, not TEST-vs-production) | **CONFIGURED — same item as the Internal beta core-loop gate above, carried forward here for this table's completeness.** The four required TEST-mode values (`TREMENDOUS_API_BASE_URL`, `TREMENDOUS_API_KEY`, `TREMENDOUS_FUNDING_SOURCE_ID`, `TREMENDOUS_CAMPAIGN_ID`) are now set on the hosted project. The full failure→reward chain has not yet been observed end to end against the hosted project — see the core-loop gate above. |
| UGC / social-networking compliance (Apple App Review Guideline 1.2) | **RESOLVED — all four items now shipped.** Guideline 1.2 expects: (1) a mechanism to filter objectionable content — **IMPLEMENTED**, a trusted, server-side deny-list check on display names and the challenge behavior/completion-definition/recipient-name text that becomes visible to Kin, enforced at the actual write boundary (`private.contains_disallowed_content`, `supabase/migrations/20260902000000_social_reports_and_content_filter.sql`), not just client-side; (2) a mechanism for users to report objectionable content — **IMPLEMENTED**, `public.submit_social_report` plus a real report affordance in `app/home/kin.tsx` (both from the activity feed and from a Kin's profile), backed by a `service_role`-only `private.social_reports` table; (3) the ability to block abusive users — **ALREADY IMPLEMENTED**, `block_kin`, tested, unchanged by this pass; (4) published contact information — **RESOLVED**, `EXPO_PUBLIC_SUPPORT_EMAIL` is now `support@kinwin.app` (see Support/contact surface above). See "Beta social-safety operations" below for the report-review workflow and the content filter's real, narrow scope. |
| Privacy policy (final, publication-ready, satisfying Guideline 5.1.1) | **NOT A BLOCKER for continued internal/ad-hoc founder testing; NARROWED for a TestFlight distribution-model decision.** Apple's Guideline 2.2 treats TestFlight-distributed builds as subject to App Review compliance, and Guideline 5.1.1 requires a real privacy policy that clearly and explicitly covers what data is collected, how, all uses, relevant third-party sharing/protection, retention/deletion policies, and how a user can revoke consent and/or request deletion. **Apple's rule does not say the policy must have been reviewed by a lawyer.** Account deletion is now real (see the Account deletion row below), and `app/legal/privacy.tsx`'s "Data retention & deletion" section now describes it — the remaining gap is that `app/legal/privacy.tsx` still explicitly self-labels as a factual DRAFT, not a finished, publication-ready policy; that self-label, not a missing feature, is what still needs resolving before TestFlight/public submission. (Separately, legal review of the policy's wording is recommended before public real-money launch given Kinwin's unusual consequence/payment/reward model — that is Kinwin legal readiness, not an Apple technical submission requirement; see the Public App Store gate table below.) |

## Public App Store gate

*True requirements for public release — beyond internal/external beta.*

| Item | Status |
|---|---|
| Privacy policy | **PUBLIC LAUNCH BLOCKER, narrowed further this pass.** A factual DRAFT now exists (`app/legal/privacy.tsx`, publicly reachable without sign-in, linked from Account settings), built strictly from `docs/PRIVACY_DATA_INVENTORY.md`'s evidence-based data inventory, and its "Data retention & deletion" section now accurately describes real, implemented account deletion. The remaining blocker is narrower than before: it is not yet a **finished, publication-ready policy satisfying Guideline 5.1.1** — it explicitly self-labels as a draft — not that it lacks a lawyer's sign-off (Apple's rule does not require that) and not that retention/deletion is unaddressed (it now is addressed, factually). A real legal review of the final wording is separately recommended before public real-money launch, given Kinwin's unusual consequence/payment/reward model; that is Kinwin legal readiness, not itself the Apple technical requirement. |
| Terms of Service (custom Kinwin Terms/EULA) | **NOT a generic Apple technical requirement — corrected framing.** Apple provides a standard EULA that applies automatically to App Store Connect submissions when no custom EULA is supplied, so a custom Kinwin Terms document is not universally required just to publish an app. Whether Kinwin's unusual consequence/payment/reward model justifies or requires its own Terms before real-money public launch is a separate **Kinwin legal/business readiness question**, not resolved here and not something engineering should assert either way — it needs actual legal review. The previous unlinked internal-placeholder route (`app/legal/terms.tsx`, labeled "NOT READY: INTERNAL PLACEHOLDER ONLY") has been **removed from the shipping app** in this pass: Apple's App Review guidance says submissions should not contain placeholder/incomplete/dormant content, and there was no product need to ship it given Apple's standard-EULA fallback. See `docs/PRODUCT_DECISIONS.md`'s consequence-consent gap analysis for what the current in-app disclosure already covers versus what a real Terms document, if legal review decides one is needed, would still need to add. |
| **Account deletion** | **RESOLVED for App Store Review Guideline 5.1.1(v)'s in-app requirement.** A real, server-backed deletion path now exists: `app/account/delete-account.tsx` (a deliberately low-prominence but discoverable entry from Account, a server-checked eligibility preflight, an explicit destructive confirmation, and a real completion state), backed by `supabase/functions/delete-account` and `supabase/migrations/20260903000000_account_deletion.sql`. Deletion is blocked outright while any challenge/payment/reward obligation is non-terminal (never an escape hatch from a commitment); once genuinely terminal, the account's own challenge history, check-ins, Playbook entries, social activity, Kin connections, and (TEST-only) Stripe/Tremendous references are hard-deleted, then `auth.users` itself, via the Supabase Admin API. See `docs/ACCOUNT_DELETION_DECISIONS.md` for the founder decisions this implements, and its "Payment / provider records" section for the one explicitly deferred piece: production, real-money retention of provider records is **not** solved by this pass and remains a genuine founder/legal decision before real-money launch. Test coverage: `supabase/tests/340_account_deletion.sql` (native Postgres) and `supabase/tests/e2e/account-deletion.e2e.ts` (real GoTrue/PostgREST/Edge Runtime, including the Admin API call). |
| UGC / social-networking compliance (Apple App Review Guideline 1.2) | **RESOLVED — same item as the External beta gate row above, carried forward here for this table's completeness.** Filtering, reporting, blocking, and published contact info are all implemented. |
| `ITSAppUsesNonExemptEncryption` + export compliance | **RESOLVED** — same item as the external-beta gate above; carried forward here only for this table's own completeness. |
| App Store Connect metadata, screenshots, privacy-nutrition-label declarations | **PUBLIC LAUNCH BLOCKER.** None exist yet — confirmed no relevant files anywhere in the repo. A working App Store Privacy declaration map now exists in `docs/PRIVACY_DATA_INVENTORY.md` to prepare for the real questionnaire, but it is not itself a submission. Open submission-time classification items in that map include: Usage Data/Product Interaction (whether check-in/reaction/source-field data needs a second declaration alongside User Content), Other Financial Info for the stake amount (whether it fits Apple's actual salary/assets/debts-style examples), and Payment Info (the exact current treatment of a third-party payment-processor SDK that never gives Kinwin raw card data). |
| Production bundle identifier | **PUBLIC LAUNCH BLOCKER.** Current identifier (`com.kinwin.mobile.beta`) is explicitly, deliberately not the final one — `docs/IOS_BETA_BUILD.md`: *"Confirm the production identifier before any production signing or listing."* |
| Production Supabase / Stripe / Tremendous environments | **PUBLIC LAUNCH BLOCKER, if real money is intended at launch.** None exist today; every current environment and every hard-coded validation in the codebase currently *rejects* anything but TEST/sandbox. This is a deliberate, correct safety property right now, and becomes a real provisioning + code-change task (e.g. `adapter.ts`'s sandbox-URL check needs a production path added, deliberately, when that day comes) at public launch. |
| Membership/subscription billing + payment-platform choice | **PRODUCT DECISION, and a launch blocker only if public launch is paid.** `domain/membership/` today is types and a pure access-mode function only — no Stripe subscription, no App Store/Play billing webhook exists. Do not assume Stripe subscriptions are valid App Store architecture for this; that needs a dedicated current-rules review before any implementation, exactly as scoped for this audit — not decided here. |
| Currency (USD only) | **PRODUCT DECISION, not automatically a blocker.** `SUPPORTED_CURRENCIES = ['USD']`, enforced at the database level via `CHECK` constraints across four migrations, plus hardcoded `$`/`en-US` formatting in the review screen. Deliberate current constraint per `docs/PRODUCT_DECISIONS.md`. Fine to launch USD-only in a USD market; blocks a SEK/multi-currency launch until addressed. |

## Product decisions before public launch

Decisions where engineering should not guess:

1. ~~Account deletion semantics.~~ **DECIDED AND IMPLEMENTED** — see `docs/ACCOUNT_DELETION_DECISIONS.md`'s "Founder decisions — as made, and what is implemented" section and the Account deletion row above. One piece remains a genuine, deliberately deferred decision: **the design and retention period of a minimal retained-record structure for production, real-money Stripe/Tremendous provider references** — the current TEST-beta pass hard-deletes every provider reference along with the rest of the account, which is correct only because no real money moves yet; this needs real legal/accounting advice before real-money launch, not an engineering guess.
2. **Payment-platform architecture for any future membership/subscription billing.** Requires a dedicated review of current Apple/Google in-app-purchase rules before implementation — do not default to a Stripe subscription without that review.
3. **Distribution model for the external beta** — ad-hoc device registration vs. TestFlight (see External beta gate table). Account deletion is already resolved regardless of this choice; this decision now only determines whether a finished, publication-ready Privacy policy becomes required before the next external-testing step, not only at final public launch. UGC/social compliance is already fully resolved and not gated on this choice.
4. **Currency scope for public launch** — USD-only vs. SEK vs. multi-currency.
5. **Whether public launch is free or paid** — determines whether membership billing is actually a launch blocker.
6. **Social scope beyond the current binary "all Kin see everything" visibility model** — is the simpler shipped model the intentional public-launch design, or does per-challenge audience control (Only me / All Kin / Selected Kin) need to ship before public users are exposed to it? The current model is a legitimate, smaller V1 (see below) — this is a call about user expectations at public-launch privacy stakes, not a technical necessity.
7. **Final production iOS bundle identifier.** Current identifier (`com.kinwin.mobile.beta`) is explicitly, deliberately not the final one — a real founder/product/release naming decision.

## Explicitly not required for first launch

The old `SOCIAL_V1_SPEC.md` scopes a much larger social product than what's shipped. None of the following have any real implementation (confirmed: not even in the disconnected prototype, for most of these) — none of them need to block internal beta, external beta, or a first public launch:

- **Comments/replies on challenges or activity** — zero real code or schema anywhere; only a lone unused type definition in the prototype.
- **Challenge Rooms** — zero implementation; only a UI-copy string ("room settings, mute, report, and block") and one unreachable prototype screen.
- **Group / multi-participant "Kin Challenges"** — zero code or schema anywhere, including the prototype. A challenge still has exactly one owner today.
- **Late joining / voting** — zero code trace anywhere, including the prototype. Pure spec idea.
- **Per-challenge audience controls (Only me / All Kin / Selected Kin) and detail-level controls (Exact / General / Progress-only)** — implemented only in the disconnected prototype (`lib/social/projection.ts`, `lib/social/challenge-audience.ts`), never wired to the real backend or navigation. The shipped product intentionally launched with a simpler binary model instead — a legitimate smaller V1, not an oversight (see product decision #6 above for whether that's the final call for public launch).
- **Social profiles / challenge history pages** — not built.
- **Push notifications** — no `expo-notifications` usage anywhere.
- **Analytics** — no analytics SDK integrated anywhere.
- **Formal abuse/reporting system** — **RESOLVED, no longer belongs on this "not required" list.** A relationship-level `block_kin` mechanism, a real report affordance (`app/home/kin.tsx`, backed by `public.submit_social_report` and `private.social_reports`), and a trusted server-side content filter at the points user-authored text becomes visible to Kin are all shipped — see the UGC / social-networking compliance row in the External beta gate and Public App Store gate tables above, and "Beta social-safety operations" below. This corrects the earlier framing here, which wrongly treated the whole area as safely deferrable "at this network size/stage" — Apple's Guideline 1.2 carries no such exemption.

## Beta social-safety operations

*What actually exists today for the real, shipped Kin/social surface — deliberately minimal, not an enterprise moderation platform.*

**Content filtering.** A small, fixed deny-list check (`private.contains_disallowed_content`, `supabase/migrations/20260902000000_social_reports_and_content_filter.sql`) runs at the exact server-side write boundaries where user-authored text becomes visible to another real person: `profiles.display_name` (any Kin, or anyone via person search), and the challenge behavior/completion-definition/recipient-name text captured once into `activation_snapshot` at activation (immutable afterward, so this one check covers `challenge_started`/`succeeded`/`failed` activity for the challenge's whole lifetime). Disallowed text is rejected outright with a plain validation error — never silently rewritten. This is intentionally narrow: it targets obvious profanity/harassment, not context, sarcasm, or every language, and does not materially change Kinwin's normal friendly-roasting tone (the deny-list itself was tested against exactly that — see `supabase/tests/330_social_reports_and_content_filter.sql`'s `friendly_roast_display_name_is_allowed` case). **Known, honest gap:** hate-speech-specific slurs are not hardcoded into this MVP list; a maintained term list for that is a real, separate decision this pass does not make.

**Reporting.** `app/home/kin.tsx` offers a "Report" action from an activity item (a small flag icon in the activity card) and from a Kin's profile (the existing manage sheet, alongside Remove Kin and Block), backed by `public.submit_social_report`. Every submission is independently re-checked server-side, never trusted from the client: an activity-level report requires exactly the visibility `social_activity`'s own RLS already enforces (owner or accepted Kin); a profile-level report (no specific activity) requires exactly the visibility `profiles_select_kin` already enforces (a `pending` or `accepted` `kin_connections` row — not a stale `removed` or `blocked` one). A caller can never submit as anyone but themselves, either way. Reports live in `private.social_reports`, a `service_role`-only table — no client, mobile or web, can ever read another person's reports, or even their own. Dedupe is scoped to **open** reports only: resubmitting against the same target while an earlier report is still open is a safe no-op (`already_reported`, not a new row), but once that report is resolved or dismissed, a genuinely new incident against the same target can be reported again — this prevents duplicate-click spam without permanently blocking future reports. `reported_activity_id` is stored as an opaque historical value, not a live foreign key, specifically so a later social-activity deletion can never fail, get blocked by, or silently mutate an existing report.

**Blocking.** Unchanged — `block_kin` (shipped earlier) already correctly cuts off social visibility the moment a connection's status leaves `'accepted'`, since every Kin-visibility RLS policy (`social_activity`, `activity_reactions`, `profiles_select_kin`, `get_kin_current_challenges`) keys off that same status check, re-evaluated live on every read. Traced directly against the schema for this pass; no hole found, so nothing was changed.

**Operational report review (beta).** No admin dashboard or new RPC exists for this, deliberately — it would be over-scoped for a beta. The founder/operator reviews open reports directly via a `service_role` session (e.g. Supabase Studio's SQL editor on the hosted project), the same access level already used for everything else operational in this project:

```sql
-- List open reports, oldest first.
select id, reporter_user_id, reported_user_id, reported_activity_id, reason, detail, created_at
from private.social_reports
where status = 'open'
order by created_at asc;

-- Mark one reviewed (status and resolved_at must be set together — see the table's own check constraints).
update private.social_reports
set status = 'resolved', resolved_at = now() -- or status = 'dismissed'
where id = '<report id>';
```

Resolving (or dismissing) a report is also what frees that same reporter+target pair to produce a genuinely new report later — the uniqueness constraint behind `already_reported` only applies while a report is still `open`.

## Known documentation drift

Do not treat these documents' status prose as current implementation truth:

- **`README.md`** — still says *"the project contains only a temporary landing page; no backend, sign-in, or payment integration exists yet"* and repeats this under "Not implemented yet." Last touched at the very first commit (`9f1e316`); never updated since. A one-line correction is included in this PR (see below) — the file is not otherwise rewritten.
- **`docs/PRODUCT_DECISIONS.md`**, "Current scope" section — claims *"No Stripe charging, Tremendous fulfillment, analytics, or push notifications are connected yet"* and that real activation *"remains unimplemented"*; both are contradicted by shipped code (`20260823000000_stripe_failure_charging.sql`, `20260827000000_tremendous_sandbox_fulfillment.sql`, `20260811000000_full_activation.sql`). It also has no mention at all of the Kin/social feature, which has since shipped.
- **`BACKEND_IMPLEMENTATION_PLAN.md`** — several phase write-ups are stale in a document whose other phases are accurate: Phase 3b (full activation) is written in future tense despite being implemented; Phase 6 explicitly (and incorrectly) claims `evaluateChallenge` "is currently a deliberate stub that always returns `evaluable: false`" — it is a complete, real evaluator; Phases 5, 9, and 10 (check-in append, reward fulfillment, invitations) read as not-started but are fully implemented and deployed. Phases 1–4 and 7 read as accurate.
- **`SOCIAL_V1_SPEC.md`** — line 3's *"Implementation has not started"* is misleading without the context above: it's accurate for the specific (disconnected, prototype) code path the spec describes, but a real, smaller-scoped Kin feature inspired by it has separately shipped to production.
- **Authoritative and current**: `docs/BETA_TEST_ENVIRONMENT.md` and `docs/IOS_BETA_BUILD.md` — cross-consistent, present-tense, and confirmed accurate against `app.config.js`, `eas.json`, and the deployed Edge Function list. Neither yet mentions the EAS Hosting origin (`kinwin-beta.expo.app`) established this session — not a contradiction, just not yet caught up.
- **Correctly self-labeled as prototypes, not stale**: `docs/SOCIAL_ONBOARDING_UX.md` and `docs/SOCIAL_UX_V1.md` both open with *"Nothing here is production code."*
- **Other docs checked and found current/consistent, no action needed**: `docs/RECIPIENT_INVITATION_ACCESS.md`, `docs/SCHEDULED_CHALLENGE_COMPLETION.md`, `docs/SUPABASE_SCHEMA.md`, `docs/TREMENDOUS_SANDBOX_FULFILLMENT.md`, `docs/PAYMENT_SETUP.md`, `docs/CHALLENGE_CHECKIN_UX.md`, `docs/PRODUCTION_DATA_MODEL.md`, `docs/STRIPE_FAILURE_CHARGING.md`, `docs/CHECK_IN_ENGINE.md`.

## Canonical end-to-end launch smoke flow

This mirrors `docs/BETA_TEST_ENVIRONMENT.md`'s own "Canonical beta smoke scenario" (its source of truth; reproduced here in the same order, with the reward-organizer terminology it uses). Recipient/organizer access is shared and the **canonical organizer** accepts *before* failure — not after, as a naive reading of the feature list might suggest — because acceptance gates the reward *handoff*, not challenge *activation*:

1. Sign up.
2. Create a challenge draft, add one or more recipients, and choose the **canonical organizer**.
3. Complete Stripe TEST payment-method setup and verify authorization via the signed webhook.
4. Activate the challenge.
5. Share scoped recipient/organizer access.
6. Have the canonical organizer accept. Recipient/organizer response never gates activation — activation already happened in step 4 — but an **accepted** canonical organizer is required later for the reward handoff.
7. Complete real check-ins and genuinely reach failure through ordinary product behavior (there is no hosted force-failure API).
8. Observe the scheduled Stripe TEST charge and its independently verified webhook success.
9. Observe exactly one Tremendous Testflight fulfillment obligation/order, with stable `external_id` idempotency (requires the Tremendous Testflight runtime configuration above).
10. Observe reconciliation reach LINK `SUCCEEDED`.
11. The owner's Home/result screen reflects the correct reward-progress state ("owner sits out" of the reward itself, per `docs/PRODUCT_DECISIONS.md`'s terminology — the owner never gets access to it).
12. The accepted canonical organizer explicitly presses **Open reward**. Kinwin only knows it generated/opened a Tremendous LINK — it does not, and cannot, claim actual redemption, usage, or attendance beyond that.
13. Confirm the owner, an ordinary non-organizer recipient, and a wrong/declined/rotated token all cannot obtain organizer reward access.

## Next recommended work packages

Ordered, prioritizing shipping over infrastructure:

1. **Unblock internal iPhone beta, then unblock the full core loop.** Apple Developer Program enrollment is now approved (see `docs/AI_HANDOFF.md`) — the remaining steps are: re-trigger the existing GitHub Actions workflow (`.github/workflows/eas-beta-release.yml`) to get current evidence of the `eas build` step (its last known result, 2026-08-16, predates enrollment approval and should not be assumed to still apply); if it still needs credentials, run the one-time interactive `eas credentials` setup; register the founder's test device(s) (`eas device:create`) if needed. The four Tremendous Testflight runtime secrets are now configured on the hosted TEST project — once a device build exists, walk the full canonical smoke flow above on a real iPhone to actually observe the failure→reward chain for the first time, including Universal Links once the real Apple Team ID is known and the AASA is redeployed.
2. **Remaining external-beta verification.** Password reset's real hosted round trip is now verified (2026-08-15). The support/contact surface (now `support@kinwin.app`), export-compliance declaration, and the TEST-mode payment disclosure copy are all shipped and configured. What's left is verification, not configuration: observe the Tremendous failure→reward chain end to end on the hosted project (see the rows above) — that has not actually been exercised yet.
3. **Public-launch package, after the remaining product decisions above are answered.** Account deletion is now implemented (see the Account deletion row above) and no longer part of this package, except for the one deferred piece — production, real-money provider-record retention design, which does need real legal/accounting advice before real-money launch. What remains: App Store Connect setup (metadata, screenshots, privacy nutrition labels using the working map in `docs/PRIVACY_DATA_INVENTORY.md`, production bundle identifier), finishing the privacy policy into a publication-ready document satisfying Guideline 5.1.1 (route infrastructure exists — `app/legal/privacy.tsx`, and its retention/deletion content is now factually complete) plus a recommended legal review of its final wording, a legal/business decision on whether Kinwin needs its own Terms document at all given Apple's standard-EULA fallback, legal review of the consequence-consent/payment/reward copy before real money is at stake, and the production-environment provisioning decision (Supabase/Stripe/Tremendous) — each still gated on an explicit founder decision, not an engineering guess.
